const handle = require("../index.ts");

// run the following in helper folder with the following cmd: yarn ts-node ./helper/localSetup.ts
// updating db with the api call results
handle.handler();
// updating db with the current rank values for the weekly rankValues
//handle.weeklyHandler();
